spring:
  datasource:
    url: jdbc:postgresql://localhost:8081/ShortnerURLTests
    username: postgres
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect